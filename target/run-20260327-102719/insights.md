# What Worked

- Reusing the existing `Milestone 6a` block was cleaner than adding another overlapping checklist section.
- The council process converged quickly once the prior insights were re-read: honest evidence gates and repo hygiene were the dominant constraints.
- A tiny docs-governance test is enough to keep this new contract from silently drifting later.
- The reusable `hellsd-gateway` lesson was still layered real-stack validation, not its script-heavy execution style.

# What Failed

- The user's original “commit disk and creds” objective does not fit sane repo hygiene for this repo.
- A loose checklist block without retrieval, redaction, and identity binding would have created checklist theater instead of an auditable manual lane.

# What To Avoid Next Time

- Do not let AGENTS manual-lab rows become a long runbook.
- Do not treat video files, hashes, or timestamps alone as proof unless they bind back to `run_id`, `session_id`, and `vm_lease_id`.
- Do not normalize raw `.qcow2`, overlays, memory dumps, or plaintext credentials into normal git history.

# Promising Next Directions

- Add a small supporting manifest or retrieval checker later if the manual-headed lane starts to run in practice.
- When a real headed Tiny11 walkthrough is authorized and available, execute it against this stricter contract rather than inventing a second evidence scheme.
