# BS-23 Plan

## Hypothesis

If the repo exposes the existing `xfreerdp` `Rfx` lane through a small reusable contract helper and proves the exact emitted flags against a same-day archived control companion, `BS-23` can close without adding a second reducer or comparison surface.

## Steps

1. Re-read recent `target/*/insights.md` files and summarize what worked, what failed, and which dead ends to avoid.
2. Review Guacamole's RDP settings and connection flow for explicit graphics-policy cues that may apply to the black-screen matrix.
3. Run a 3-agent council with independent proposals, adversarial review, refinement, detailed plans, and evidence-based voting.
4. Implement the winning plan by reusing the existing `ManualLabXfreerdpGraphicsMode::Rfx` path and exposing only the minimum helper needed for integration proof.
5. Add an integration test that proves the exact `xfreerdp-rfx` lane identity, exact codec flags, and same-day control comparison contract.
6. Extend the canonical runbook and docs-policy test so the operator contract names the sanctioned `BS-23` lane and archived-control prerequisite.
7. Run focused verification, then the baseline formatting, clippy, and integration-test path.
8. Record the run artifacts, commit the save point, and update `AGENTS.md`.

## Assumptions

- `BS-23` only needs a bounded contract proof, not a fresh runtime experiment lane or reducer.
- The existing `control_run_comparison_summary` and per-session `driver_args` surfaces are sufficient evidence.
- The already established black-screen runbook is the correct canonical operator-facing seam for this rule.
- A Guacamole-inspired explicit graphics-policy approach remains preferable to heuristic narration here.
